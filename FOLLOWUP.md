# 补充确认说明

本次补充实际用时：约 40 分钟（阅读问题、核对现有实现、撰写和复查）。

## 一、系统负责人补充

### 1. 如果这套系统后续由我负责

我会把当前系统判断为“可用于受控演示和方案验证，但不能直接作为生产系统交付”。

当前已经可用的部分是：混合 PDF 能逐页路由原生解析与 OCR；财务表格单元格保留期间、行列、页码、坐标和原值；问答能返回引用，并对无答案、缺少期间和 OCR 异常做拒答、澄清或复核提示；上传失败可回滚，上传新文档后旧证据会失效。以上能力已有真实 PDF、18 个自动化测试和 10 条黄金问题验证。

不能放心交付的部分是：当前表格恢复规则仍与样本结构相关；OCR 依赖 macOS Vision，且扫描页已有低置信度和非法数字；黄金集规模小、文档类型单一；系统没有鉴权、多租户隔离、异步任务和人工复核后台。

下一步我会优先扩充有标注的真实文档与 bad case 集，并以“数字精确值、引用页码、回答状态”建立基线。原因是先知道错误集中在哪一层，才能决定优先改 OCR、结构解析还是检索，避免凭感觉重构。之后再处理可部署 OCR、人工复核入口和生产安全能力。

### 2. 如果要给客户试用

我会先确认以下问题，并形成一页验收说明：

- 谁使用、最常问什么问题，结果用于辅助查询还是直接进入业务决策。
- 文档类型、版式变化、语言、页数、扫描质量、更新频率和数据量。
- 错答、拒答和人工复核各自的成本；哪些字段属于“错一次也不可接受”。
- 是否允许外部模型/OCR，数据部署位置、权限、保留周期和审计要求。
- 验收样本由谁提供，验收指标和失败处理方式是什么。

第一版不会承诺“支持所有 PDF”。我会选择客户最高频的一类文档和 20-50 个代表性问题，交付上传、解析、证据问答、引用、拒答和复核闭环。验收至少同时检查数字精确匹配、引用正确、可回答性判断和高风险错误是否被拦截。

### 3. 如果要保证质量和稳定性

我会分层定义指标，而不是只统计回答正确率：

| 层级 | 主要指标 |
| --- | --- |
| 页面与结构解析 | 页面类型路由准确率、字段/单元格精确匹配率、表格归属准确率 |
| 问答 | 可回答性分类准确率、数字 Exact Match、引用页准确率 |
| 风险控制 | 高风险幻觉数、异常 OCR 的 `needs_review` 召回率、旧知识库污染数 |
| 运行 | 解析成功率、P95 耗时、失败回滚成功率、数据隔离与审计检查 |

演示或生产前出现问题时，我会先冻结相关变更并判断影响范围。如果涉及错误财务数字、数据泄露或旧知识库污染，按 P0 处理：关闭受影响能力或回退上一版证据库，明确告知影响和临时方案。随后用 `diagnostics.json`、`cells.jsonl`、`validation_report.json` 和 `events.jsonl` 判断问题在解析、结构化、检索还是回答阶段，构造最小复现，修复后将该 case 加入回归集。只有原失败 case、同类切片和完整回归都通过，才认为修复有效。

### 4. 如果时间有限

我会按“客户影响 × 错误代价 × 是否可回退”排序：

- P0：错误数字被当作可信答案、数据安全、文档切换污染、服务不可恢复。
- P1：高频问题失败、引用错误、明显性能问题。
- P2：低频版式、体验优化和非关键功能。

关键节点前冻结非必要修改，先保证一条可稳定演示的端到端路径和明确的降级方案。风险同步会包含：现象、影响范围、当前判断、临时措施、负责人和下次更新时间。需要协调资源时，我会把可独立验证的任务拆开，例如一人复现和整理 bad case，一人定位 OCR/解析，另一人做回归与演示检查；最终发布判断由负责人统一收口。

### 5. 我会如何使用 AI 或智能体工具

我会让 AI 协助候选方案分析、代码初稿、测试样例扩展、日志聚类、bad case 分类和文档整理。对明确规则的批量任务，可以交给 Agent 执行并生成变更说明。

以下结果必须由我自己把关：需求与验收边界、架构取舍、财务数字及引用、数据安全、测试是否真的覆盖风险，以及是否允许发布。检查 AI 输出时，我会要求它给出可验证结果，再通过真实样本、反例、黄金集、代码审查和完整回归确认。AI 生成的解释不能作为财务事实来源，也不能因为测试由 AI 编写就默认测试正确。

### 6. 一个真实的闭环例子

我选择这次作业作为最近且可核验的真实例子。题目称附件为扫描 PDF，最初很容易直接采用全量 OCR。我先检查每页文本层和图片，发现第 1-5 页有文本层，只有第 6 页需要 OCR，因此改成逐页路由。继续检查后又发现，原生文本虽然能提取数字，但会丢失表格行列关系，所以我没有停在全文 RAG，而是改为结构化单元格证据。

扫描页随后出现 `26.075,352,739.73` 这类异常数字。我判断自动猜测修复会制造财务事实，因此保留原值、增加格式和置信度检查，并把回答降为 `needs_review`。上传功能完成后，我又增加反向测试：上传一份不含财务表格的新 PDF，再询问旧问题，必须返回 `no_answer`，用来证明知识库确实切换。

这几次判断都同步记录在设计、测试和工程决策文档中，并转化为自动化测试。最终结果是 18 个测试通过、黄金问题集 10/10；同时没有隐藏第 6 页 OCR 仍存在的风险。这个过程体现了我的处理方式：先观察真实输入，明确最危险的错误，再把判断变成可复现的验证和交付边界。

### 7. 如果文档章节层级解析不对

我会先区分错误来源：OCR 是否漏字、阅读顺序是否错误、标题层级是否错、表格是否归错章节、跨页表格是否被截断，还是后续切块丢失了父子关系。然后从受影响问答反查到引用页、坐标和解析结果，按文档版式与错误类型建立 bad case 切片，判断是个别模板问题还是通用问题。

短期交付时，我会保留页码、坐标和相邻上下文；对无法确认归属的内容降低置信度，使用页面级证据或人工复核，避免错误层级继续传播到答案。长期方案会将章节树和证据对象分离，结合版面特征、编号规则和跨页关系建立结构解析器，并保留解析版本。

验证时不仅看章节树本身，还看下游效果。结构层指标包括标题层级准确率、父子边 F1、表格归属准确率和跨页合并准确率；下游指标包括检索召回、数字 Exact Match 和引用准确率。修复必须在独立保留集和原有回归集上均不退化。

### 8. 如果系统效果不稳定

当前最可能影响效果的是“扫描表格 OCR 错误与行列错配”。现有第 6 页已经出现低置信度、非法数字格式和勾稽失败，是明确的 bad case 来源。

我会先按原生页/扫描页、表格类型、问题类型和回答状态切分评估集，记录数字 Exact Match、引用准确率、错误可信回答数和 `needs_review` 召回率。每个 bad case 追踪到原图区域、OCR 原值、结构化单元格、检索结果和最终回答，以判断错误来自识别、表格恢复还是回答状态。

优化时一次只改变一个主要变量，例如 OCR 引擎、图像预处理、行列匹配规则或置信度阈值，并与固定基线对比。不能只看平均正确率：对财务场景，我更关注“错误数字被标记为可信”的数量是否为零，以及改进是否只是通过更多拒答换来的。新方案只有在保留集提升、关键风险指标达标、原生页和原有黄金集不回退后才会发布。

## 二、工作方式补充

### 1. 遇到不清楚但又比较急的需求

我会先确认目标用户、必须解决的一个核心问题、输入输出样例、不可接受的错误和最晚决策时间。若对方暂时无法回答，我会明确写下假设与风险，用 30-60 分钟做最小验证，再交付一条可演示的纵向流程，而不是同时铺开所有功能。第一版同时附带“已完成、未覆盖、需要确认”的清单，并约定短反馈节点，避免到最后才发现方向偏了。

### 2. 关键节点前发现问题

我先判断问题是否影响核心演示、数据正确性、安全或可恢复性。P0 问题优先止损：冻结变更、准备稳定版本或关闭风险功能，并立即同步影响、临时方案和下一次更新时间。随后安排最小复现、定位、修复和独立回归，演示前再完整走查一次。我会投入到风险被消除或已有可信降级方案为止，但不会靠隐瞒风险或未经验证的临时修改换取表面按时。

### 3. 沟通中出现分歧

我会把分歧从观点转成可验证的问题：各方期望的输入、输出、验收样例和错误代价分别是什么。能实验的就用小样本或原型验证；不能实验的明确决策人和截止时间。讨论中记录已确认项、未决项和负责人，先推进无争议部分。即使有人着急，也保持事实、选项和影响清晰，避免在同一个抽象表述上反复拉扯。

### 4. 多件事情同时压过来

我会先确定关键路径和阻塞关系，把高风险验证放在前面，并限制同时进行的任务数量。开发、排查、测试和文档中，可并行的工作拆成明确产物和完成标准。AI/Agent 可以帮助搜索代码、生成候选测试、汇总日志、整理文档和执行重复检查；我一定会亲自复查需求解释、根因判断、关键代码、数据结果、测试断言和最终对外说明。

### 5. 一个真实例子

这次作业中，我没有按“扫描 PDF”这一描述直接全量 OCR，而是先检查真实文件并调整方案；也没有为了演示效果自动修正异常财务数字，而是明确暴露风险并增加复核状态。随后我用黄金集、反向上传测试和失败回滚测试把判断变成可重复验证的结果。面对有限时间，我优先完成会影响答案可信度的证据、引用、拒答和测试闭环，并在文档中如实说明未完成的生产能力，而不是继续增加表面功能。
